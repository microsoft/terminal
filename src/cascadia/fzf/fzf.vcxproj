<?xml version="1.0" encoding="utf-8"?>
<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup Label="Globals">
    <ProjectGuid>{FDDBD0EB-0D95-4B92-A352-D4859064B1C3}</ProjectGuid>
    <Keyword>Win32Proj</Keyword>
    <RootNamespace>fzf</RootNamespace>
    <ProjectName>fzf</ProjectName>
    <TargetName>fzf</TargetName>
    <ConfigurationType>StaticLibrary</ConfigurationType>
    <SubSystem>Console</SubSystem>
    <OpenConsoleUniversalApp>true</OpenConsoleUniversalApp>
  </PropertyGroup>
  <Import Project="..\..\..\common.openconsole.props" Condition="'$(OpenConsoleDir)'==''" />
  <Import Project="$(OpenConsoleDir)src\common.nugetversions.props" />
  <Import Project="$(OpenConsoleDir)src\common.build.pre.props" />
  <ItemGroup>
    <ClInclude Include="fzf.h" />
  </ItemGroup>
  <ItemGroup>
    <ClCompile Include="fzf.cpp" />
  </ItemGroup>
  <ItemDefinitionGroup>
    <ClCompile>
      <!-- It's a project containing a single cpp file; we don't need a pch. -->
      <PrecompiledHeader>NotUsing</PrecompiledHeader>
    </ClCompile>
  </ItemDefinitionGroup>
  <!-- ========================= Globals ======================== -->
  <Import Project="$(OpenConsoleDir)src\common.build.post.props" />
  <Import Project="$(OpenConsoleDir)src\common.nugetversions.targets" />
</Project>
